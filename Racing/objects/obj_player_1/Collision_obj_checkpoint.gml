/// @description Keeps track of passed checkpoints

inst_num = instance_place(x,y,obj_checkpoint);

for(i = 0; i < instance_number(obj_checkpoint);i++){
	if(inst_num = obj_checkpoint_counter.check_list[i]){
		check_check[i] = 1;	
	}
}

